# Postgres (используется в docker-compose)
DATABASE_URL=postgresql+psycopg2://approval:approval@db:5432/approval
# Для локального запуска без Docker можно использовать SQLite:
# DATABASE_URL=sqlite:///./local.db
ENV=local
LOG_LEVEL=INFO
